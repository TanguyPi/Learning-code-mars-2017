// Créer un tableau qui contient 3 prénoms
var users = [John,Lea,Lora,Ringo];

    // Faire une boucle for sur le tableau pour saluer chacun des prénoms
    for(var i = 0; i < users.length; i++){
        // Code à éxécuter à chaque itération (boucle)
        console.log(' Salut ' + users[i]);


    };