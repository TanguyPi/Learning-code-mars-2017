var myString ='Je suis un texte';
console.log(myString);


// Créer une variable de type NUMBER (Integrer)
var myNumber = 237;
// Pour l'envoyer dans la console directement on écrit la ligne ci-dessous sinon elle ne s'affichera pas dans la console
console.log(myNumber);

// Créer une variable de type BOOLEAN
var myBoolean = true;
console.log(myBoolean);

