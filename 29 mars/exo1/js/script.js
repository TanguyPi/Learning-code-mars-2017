// Créer un tableau contenant 4 index
        // 1 string, 1 number, 2 booleans différents

var myArray = ['Du texte, 452, true, false'];
console.log(myArray);

        // Ajouter un string dans le tableau
        myArray.push('bloblibla');
        console.log(myArray);

                // Afficher dans la console la taille du tableau
                // Afficher le premier et le dernier index du tableau dans la console
                console.log(myArray.lenght, myArray[0], myArray[4]);

                        // Créer un objet contenant 3 propriétés: le tableau, 1 prénom, 1 âge
                        var objet = {myArray: 'myArray', firstname: 'Tanguy' , age: '45'};
                        console.log(objet);

                                // Afficher toutes les propriétés de l'objet dans la console une par une
                                console.log(objet.myArray);
                                console.log(objet.firstname);
                                console.log(objet.age);

                                        // Modifier la propriété age de l'objet
                                        objet.age = '25';
                                        console.log(objet.age);
                                        
        
               
